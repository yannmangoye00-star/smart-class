spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/smartclass}
    username: ${DB_USERNAME:smartclass}
    password: ${DB_PASSWORD:smartclass}
  jpa:
    show-sql: false

jwt:
  secret: ${JWT_SECRET:smartclass-prod-secret-key}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}
